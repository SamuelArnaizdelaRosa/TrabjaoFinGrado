import { ProductosDirective } from './productos.directive';

describe('ProductosDirective', () => {
  it('should create an instance', () => {
    const directive = new ProductosDirective();
    expect(directive).toBeTruthy();
  });
});
